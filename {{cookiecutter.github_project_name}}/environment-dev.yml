name: {{cookiecutter.conda_package_name}}
channels:
  - conda-forge
dependencies:
  # Build dependencies
  - make
  - cmake
  # Host dependencies
  - xeus
  - nlohmann_json
  - cppzmq
  - xtl>=0.7, <0.8
  # Test dependencies
  - pytest
  - nbval
  - ipympl
  - jupyter_kernel_test
